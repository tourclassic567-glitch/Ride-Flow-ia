# Nixpacks Configuration

# This configures Nixpacks for Node.js deployment

builds:
  - type: "npm"
    env:
      NODE_VERSION: "16"

features:
  - install:
      packages:
        - nodejs
        - npm
